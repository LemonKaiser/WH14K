using Robust.Client.AutoGenerated;
using Robust.Client.Graphics;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Maths;

namespace Content.Client._WH40K.AccountLoad;

[GenerateTypedNameReferences]
public sealed partial class WH40KAccountLoadControl : Control
{
    private static readonly Color PanelBackground = Color.FromHex("#12151B");
    private static readonly Color PanelBorder = Color.FromHex("#5D4D2A");
    private static readonly Color ProgressBackground = Color.FromHex("#0C0D11");
    private static readonly Color ProgressFill = Color.FromHex("#D2B25A");

    public WH40KAccountLoadControl()
    {
        RobustXamlLoader.Load(this);

        LayoutContainer.SetAnchorPreset(this, LayoutContainer.LayoutPreset.Wide);
        ApplyStyles();
    }

    private void ApplyStyles()
    {
        StatusPanel.PanelOverride = new StyleBoxFlat
        {
            BackgroundColor = PanelBackground,
            BorderColor = PanelBorder,
            BorderThickness = new Thickness(2),
            ContentMarginTopOverride = 14,
            ContentMarginBottomOverride = 14,
            ContentMarginLeftOverride = 14,
            ContentMarginRightOverride = 14,
        };

        this.ProgressBar.BackgroundStyleBoxOverride = new StyleBoxFlat
        {
            BackgroundColor = ProgressBackground,
            BorderColor = PanelBorder,
            BorderThickness = new Thickness(1),
        };

        this.ProgressBar.ForegroundStyleBoxOverride = new StyleBoxFlat
        {
            BackgroundColor = ProgressFill,
        };
    }
}
