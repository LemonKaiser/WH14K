using Content.Client.Localization;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.Administration.UI.Tabs.AtmosTab
{
    [GenerateTypedNameReferences]
    public sealed partial class AtmosTab : Control, ILocalizedControl
    {
        public AtmosTab()
        {
            RobustXamlLoader.Load(this);
            Relocalize();
        }

        public void Relocalize()
        {
            AddAtmosButton.Text = Loc.GetString("admin-ui-atmos-add");
            AddGasButton.Text = Loc.GetString("admin-ui-atmos-add-gas");
            FillGasButton.Text = Loc.GetString("admin-ui-atmos-fill-gas");
            SetTemperatureButton.Text = Loc.GetString("admin-ui-atmos-set-temperature");
        }
    }
}
