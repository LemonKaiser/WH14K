using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.Lobby.UI.Loadouts;

/// <summary>
/// A button that toggles the weapon-mod list for a loadout. Needed to override default styles.
/// Shows a gear icon (⚙).
/// </summary>
[GenerateTypedNameReferences]
public sealed partial class WeaponModButton : Button
{
    private const string ClosedMark = "⚙";
    private const string OpenedMark = "⚙";

    public WeaponModButton()
    {
        RobustXamlLoader.Load(this);
        Text = ClosedMark;
    }
}
